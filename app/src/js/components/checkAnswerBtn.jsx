import React from 'react';

const CheckAnswerButton = (props) => {
    return (
        <div className="col-2">
            <button>=</button>
        </div>
    );
};

export default CheckAnswerButton;